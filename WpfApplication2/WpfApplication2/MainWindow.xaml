<Window x:Class="WpfApplication1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication1"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <StackPanel>
            <TextBlock Margin="0,0,476.4,0">身高</TextBlock>
            <TextBox Margin="42,0,80.4,0" x:Name="TText" TextChanged="TText_TextChanged">
            </TextBox>
            <Button Margin="438,0,0.4,0" x:Name="BButton" Click="BButton_Click">
                清除
            </Button>
            <Slider Minimum="0" Maximum="200" x:Name="Long" Margin="0,0,0.4,0" ValueChanged="Long_ValueChanged"/>
            <TextBlock Margin="0,0,476.4,0">體重</TextBlock>
            <TextBox Margin="42,0,80.4,0" x:Name="TTText">
            </TextBox>
            <Button Margin="438,0,0.4,0" Click="Button_Click_1">
                清除</Button>
            <Slider Minimum="0" Maximum="100" Margin="0,0,0.4,0" x:Name="LLong" ValueChanged="LLong_ValueChanged"/>
            <Button Margin="42,0,80.4,0" Click="Button_Click">計算
            </Button>
            <TextBlock x:Name="FFF" Margin="90,0,134.4,0" Height="38">0</TextBlock>
        </StackPanel>
    </Grid>
</Window>
